entity cds.xt.Jobs {
    key ID: String; // REVISIT: cuid from cds/common?
    key tenant: String(100);
    status: String(20) default 'queued';
    result: LargeString;
    timestamp: Timestamp @cds.on.insert: $now;
}

entity cds.xt.Tenants {
    key ID: String;
    metadata: String;
}
